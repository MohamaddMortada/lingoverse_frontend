name: lingoverse_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0-323.2.beta

dependencies:
  dio: ^5.8.0+1
  flutter:
    sdk: flutter
  http: ^1.3.0
  shared_preferences: ^2.5.2
  speech_to_text: ^6.3.0
  permission_handler: ^11.0.0
  flutter_tts: ^3.6.3
  image_picker: ^1.0.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
